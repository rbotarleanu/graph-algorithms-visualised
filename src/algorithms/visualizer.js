

export default class Visualizer {
    constructor(algorithm, speed, nodeRefs, edgeRefs) {
        this.algorithm = algorithm;
        this.speed = speed;
        this.nodeRefs = nodeRefs;
        this.edgeRefs = edgeRefs;
    }
}